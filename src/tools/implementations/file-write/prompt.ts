export const description = `Creates or overwrites a file at the given path with the provided content.

Parent directories are created automatically if they do not exist. If the file already exists, its contents are completely replaced.

Returns the resolved absolute path and number of bytes written. Requires user approval before executing.`;
